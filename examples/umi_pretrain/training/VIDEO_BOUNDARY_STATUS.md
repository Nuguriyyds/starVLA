# 视频边界：已修正的案例与剩余失败

本轮没有改变低维窗口、任务划分、四视图顺序或约一帧的匹配容差。

## 微秒量化案例

原样本 `126293227` / episode `34300` 的右头相机：stream time_base 为
`1/1000000000`，实际相邻 PTS 在微秒栅格；应取的 PTS 为
`286066677000`，元数据起点为 `286.066677666` 秒。

`umi_video_time.py` 用有理数计算。对于兼容微秒单位的 time_base，且候选
帧确实落在微秒栅格时，把起止边界统一向下量化到微秒，再采用半开区间。
边界变化严格小于 1 微秒，没有把两端向外各扩一帧。其他候选帧保持原始
精确边界。请求与选中帧的匹配容差保持不变。

同一候选帧对相邻区间采用同一单调映射，原本不相交的区间不会因此重叠。
小于一个微秒且量化后退化的片段报错；不推断或修复原本就重叠的元数据。
政策版本和规则已进入 Dataset provenance，从而进入训练 run identity。

原失败样本在 worker=0/2 下均取得上述正确 PTS，四路像素和定位信息一致。
对同文件的 episode 34299/34300/34301 检查首尾及越界拒绝；该帧仅属于
34300。合成测试覆盖共享边界、其他 time_base、非栅格帧和越界拒绝。

## 原 48 次首窗口压力序列仍未通过

保留原序列重跑，worker=0/2 都完成前 3 个窗口，在第 4 个失败：

- `dataset_index=193328277`，episode `52630`，frame `0`，左头相机。
- 视频：`videos/observation.images.head_left/chunk-002/file-348.mp4`。
- 元数据起点：`2383.366923991` 秒。
- 前一候选帧 PTS：`2383366909000`（`2383.366909` 秒），早 **14.991 微秒**。
- 下一帧：`2383.400351` 秒，晚 **33.427009 毫秒**，超原有匹配容差。

这不是小于一微秒的量化案例，不能由本次窄范围修复推出该帧的正确归属。
尚需核查上游合并/裁剪时视频时间与 episode 时间的映射，以及该段的原始
采集边界。没有把阈值放大到 15 微秒或一帧、自动跳过此样本、用中点序列
代替首窗口测试。工具输出 `status=failed` 并以非零状态退出。

因此本轮仅确认原微秒案例及其邻接检查通过，**不声称全量视频边界已修好，
也不声称 48 次原压力序列已通过**。完整案例见本目录
`boundary_checkpoint_validation/video_boundaries.json`。
