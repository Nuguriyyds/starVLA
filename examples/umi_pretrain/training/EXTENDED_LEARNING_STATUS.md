# 双 LN 配置：解析采样检查与 2000 更新学习试验

2026-09-20。基于 `2a654c6`，实际运行代码 `9307d7a422b0ad68721b028fe2f3eb28d369a3c7`。

本轮不增加模型机制：保留 decoder LN、条件 K/V 输入 LN、连续 state，使用同 32 个固定窗口和全模型更新。目标是检验训练预算是否不足，不再把第 50 步的软指标视为继续训练的必要条件。

## 本轮结论

2000 步固定 FM loss 为 0.120752，相对初始化下降 93.2%，同噪声 bias-only 为 1.696841；噪声端沿理想方向平均增益 1.0375。

四步生成的三项动作误差均优于保持当前姿态基线，小数据可学习性通过。下一步扩大工程样本、保留当前模型，不继续依据旧的 50 步结果修改架构。

这次检验的是更长预算及相应学习率调度的组合，不能单独分离更新次数与调度的因果贡献。无论结果如何，上一轮 50 步未通过人为筛选，都不等于已经证明模型不能学习。训练窗口与评测窗口重叠，本轮不批准直接把结果外推到全量预训练或闭环性能。

没有触发额外 16 步采样诊断；不为增加测试数量而额外运行。

## 解析速度场链路检查

取窗口 0 的归一化真值 A，使用 `(A-x)/(1-t)` 替换真实 action decoder 的最后 16 个位置；其余位置置零。保留原模型的真实噪声初始化、时间循环、动作截取、Euler 更新和 inverse_action。独立从 raw_dataset 读取未归一化标签进行比较。hooks 结束即移除，并恢复 RNG 与训练模式；不更新任何参数。

- 通过：`True`；真实采样时间 `[0.0, 0.25, 0.5, 0.75]`。
- 归一化终点最大误差：`2.9802322387695312e-08`。
- 反归一化后对原始标签最大误差：`2.0489096641540527e-08`。
- 中间状态对解析路径最大误差：`1.1920928955078125e-07`。

这是使用真值的链路测试，不是模型预测成绩。它仅排除本例中的时间方向、速度符号、步长、动作截取和反归一化错误，不证明一般数据同步或学习能力。

## 训练配置与控制变量

从与参考组一致的初始化开始，新 run 的总计划直接定义为 2000 更新；warmup 仍为 10，VLM 峰值学习率 1e-5，动作等其他参数 1e-4；每卡 batch=1、累积=2、单 PPU。32 窗口累计曝光 4000 次，相当于 125 遍；FM 每次仍重新抽噪声与时间。

**学习率衰减轨迹随总计划改变**，不是只把原 200 步已归零的循环延长。记录的第 50 步使用学习率：原计划 `[7.947368421052633e-06, 7.947368421052632e-05]`，新计划 `[9.804020100502513e-06, 9.804020100502513e-05]`。模型、数据、峰值学习率和初始参数探针对照见 `control_comparison.json`；不宣称设备计算跨运行逐位一致。

只在 0/50/200/500/1000/2000 评测全部 32 窗口，保留原有固定评测种子、四步采样与保持状态基线。bias-only loss 捕获每次真实 FM 前向的同一份噪声，而非另抽噪声或用另一时间。噪声端响应沿用 0/16/31 窗口，不保存逐层大激活。

取消 50 步软指标早停。读取失败、非有限 loss/梯度等硬故障仍由生产 trainer 报错。完整 basic checkpoint 只在 1000 和 2000 保存；没有改公共环境、视频读取、数据清单、动作表示、归一化统计或分布式实现。

## 学习曲线

| 更新 | 固定 FM loss | 同噪声 bias loss | 位置 RMSE | 夹爪 RMSE | 旋转误差 ° | 噪声反向增益均值 | cosine 均值 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 | 1.771808 | 1.696871 | 0.156388 | 0.017252 | 37.659 | -0.00109 | -0.0547 |
| 50 | 1.626441 | 1.696741 | 0.130604 | 0.017054 | 37.321 | 0.00119 | 0.7044 |
| 200 | 1.292858 | 1.696720 | 0.109895 | 0.014329 | 32.437 | 0.17186 | 0.4372 |
| 500 | 0.578638 | 1.696822 | 0.045243 | 0.010277 | 18.036 | 0.70126 | 0.8475 |
| 1000 | 0.233578 | 1.696845 | 0.022650 | 0.005471 | 6.934 | 1.03019 | 0.9790 |
| 2000 | 0.120752 | 1.696841 | 0.013165 | 0.004730 | 4.254 | 1.03749 | 0.9936 |

保持当前姿态基线：位置 **0.019161**，夹爪 **0.008730**，旋转 **6.315°**。RMSE 是原始标签数值单位。

t=0 理想速度差为 −Δ噪声；沿理想方向增益与 cosine 仅用于解释趋势，不是早期硬阈值。全部生成误差在训练窗口上计算，不代表泛化或闭环成功。

末次对保持状态的检查：`{"position_beats_hold": true, "gripper_beats_hold": true, "rotation_beats_hold": true}`。

若固定 FM loss 相对初始化下降超过一半、噪声反向增益超过 0.1，但四步生成仍未全部胜过基线，工具仅补一次同权重 16 步采样。它是推理诊断；训练及主要评测保持四步。实际是否执行及结果在 `summary.json` 中记录。

## 运行与源码

实际更新 `2000`，状态 `complete`；loss/全局梯度范数全部有限：`True`；峰值 allocated 显存 `62.18` GiB。保存耗时：`[{"name": "checkpoint_total", "host_seconds": 89.05958185100462}, {"name": "checkpoint_total", "host_seconds": 110.78742928299471}]`。

训练进程已不在运行，进度和最终 summary 均完整写出；但执行它的 SSH 连接未自动返回退出状态，关闭了本地残留 SSH 连接。**未取得训练进程退出码，不宣称已验证正常退出，也未据此断定训练清理发生崩溃。** 本轮不为此重跑训练或改环境。

本轮只新增 `examples/umi_pretrain/tools/check_qwenpi_extended_learning.py`，复用现有 trainer、FixedEvaluation、噪声端探针。生产模型结构没有改动。阅读顺序：`oracle_check()` → `ExtendedEvaluation.measure()` → `main()` 中 2000 步计划与稀疏评测。

```bash
python examples/umi_pretrain/tools/check_qwenpi_extended_learning.py --reference-run /mnt/workspace/Native_Policy/user/wyt/runs/qwenpi_condition_layernorm_v1 --output-dir 新的私人运行目录
```

使用已验证的私人 PPU 环境，在仓库根目录设置 `CUDA_VISIBLE_DEVICES=0 HF_HUB_OFFLINE=1 NO_ALBUMENTATIONS_UPDATE=1 PYTHONPATH="$PWD"`。完整权重和预测 NPZ 留在 `/mnt/workspace/Native_Policy/user/wyt/runs/qwenpi_extended_learning_v1`，提交的 evidence 不包含原始图像或动作标签。
